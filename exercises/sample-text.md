# Vim 练习样本文本 - Chapter 01 专用

这个文件用于练习 motions 和 text objects。
请按照 chapters/01-motions-and-text-objects.md 中的指示练习。

## 练习段落 1

Vim 的强大在于它的组合命令。motion 决定了你移动到哪里，operator 决定了你要做什么。
例如，`ciw` 可以快速修改一个单词，而不需要移动光标到单词开头再按 `cw`。

## 练习段落 2 (包含各种括号和引号)

这里有一些 "双引号内容" 和 '单引号内容' 需要练习 ci" 和 ci' 。
还有 (小括号里面有很多文字) 和 {大括号内容也很重要}。
HTML 示例: <div class="container">需要修改的内容在这里</div>

## 练习段落 3 - 重构目标

这个句子有很 多 多余的 空格 和 重复的 单词 ，你可以使用 motions 来清理它。
目标是使用尽可能少的按键来完成编辑。

## 练习段落 4 - 标记练习

在这里设置一个标记 `ma` ，然后移动到文件开头，再跳转回来练习 `` `a ``。

---

**练习指令**：
1. 使用 `w` `b` `e` 在单词间跳跃
2. 使用 `f(` 快速跳到括号
3. 练习 `ciw` 修改 "很多" 这个词
4. 练习 `cit` 修改 div 标签内的内容
5. 使用 `d$` 删除到行尾
6. 反复练习直到流畅

完成后可以尝试自己添加更多练习文本。

**提示**：按 `u` 撤销任何错误操作。使用 `:e!` 可以重置文件到保存状态。
